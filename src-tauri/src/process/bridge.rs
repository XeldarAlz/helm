/// Bridge handles stdin/stdout communication with a Claude Code child process.
/// Will be fully implemented in Phase 2 when ProcessManager is built out.

pub struct Bridge {
    // Will hold ChildStdin writer and stdout reader
}

impl Bridge {
    pub fn new() -> Self {
        Self {}
    }
}
